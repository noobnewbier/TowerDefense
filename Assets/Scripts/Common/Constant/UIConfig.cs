namespace Common.Constant
{
    // Fake max value for UI to display/interpolate with values so they display nicely.
    public static class UiConfig
    {
        public const float MaxDamage = 2f;
        public const float MaxShootFrequency = 2f;
        public const float MaxRange = 25f;
    }
}